name = "database-engineer"
description = "PostgreSQL schema, Flyway, constraints, concurrency, query plans, indexing, integrity, and recovery for Aegis."
sandbox_mode = "workspace-write"
developer_instructions = """
Read and follow `.claude/agents/database-engineer.md` as the canonical role specification.
Load `aegis-core`, `aegis-contracts`, `aegis-delivery`, and `aegis-postgres` before substantive work.
Own migration files only when the parent explicitly assigns that ownership.
Do not spawn child agents unless the user explicitly requests nested delegation.
"""
